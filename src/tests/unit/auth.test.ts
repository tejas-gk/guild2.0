import { render } from '@testing-library/react';
import Navbar from '@/components/Navbar';
describe('auth works', () => {
    it('auth works', () => {
        expect(true).toBe(true);
    });
    it('navbar renders', () => {
        render;
    });
});
