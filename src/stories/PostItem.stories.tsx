import React, { useState } from 'react';

export default {
    title: 'Post item',
};

export const Basic = () => {
    const [isOpen, setIsOpen] = useState(false);
    return <div className='bg-red-400'>vr</div>;
};
